# OpenAI (required for AI endpoints)
OPENAI_API_KEY=sk-...

# Same Firebase project as Flutter — service account JSON as single-line string on App Runner
FIREBASE_CREDENTIALS_JSON={"type":"service_account",...}

# Optional
# OPENAI_MODEL=gpt-3.5-turbo
